import { type ReplayScene } from "./scene";
import type { BeforeRenderCallback, CameraSettings, ReplayCameraViewMode, ReplayFreeCameraPreset, ReplayModel, ReplayPlayerPlugin, ReplayPlayerPluginDefinition, ReplayPlayerTimelineProjection, ReplayPlayerTimelineSegment, ReplayPlayerOptions, ReplayPlayerSnapshot, ReplayPlayerState, ReplayPlayerStatePatch } from "./types";
type ReplayPlayerListener = (state: ReplayPlayerState) => void;
export declare class ReplayPlayer extends EventTarget {
    readonly container: HTMLElement;
    readonly replay: ReplayModel;
    readonly options: ReplayPlayerOptions;
    readonly sceneState: ReplayScene;
    private readonly beforeRenderCallbacks;
    private readonly plugins;
    private readonly fieldScale;
    private readonly desiredCameraPosition;
    private readonly desiredLookTarget;
    private readonly boundWindowResize;
    private readonly liveGameState;
    private readonly kickoffGameState;
    private timelineSegmentsCacheKey;
    private timelineSegmentsCache;
    private resizeObserver;
    private animationFrameId;
    private disposed;
    private playing;
    private speed;
    private currentTime;
    private playbackStartedAt;
    private playbackStartedTime;
    private cameraDistanceScale;
    private customCameraSettings;
    private cameraViewMode;
    private freeCameraTransition;
    private attachedPlayerId;
    private ballCamEnabled;
    private boostMeterEnabled;
    private boostPickupAnimationEnabled;
    private hitboxWireframesEnabled;
    private hitboxOnlyModeEnabled;
    private skipPostGoalTransitionsEnabled;
    private skipKickoffsEnabled;
    constructor(container: HTMLElement, replay: ReplayModel, options?: ReplayPlayerOptions);
    play(): void;
    pause(): void;
    togglePlayback(): void;
    setPlaybackRate(speed: number): void;
    setCameraDistanceScale(scale: number): void;
    setCustomCameraSettings(settings: CameraSettings | null): void;
    setAttachedPlayer(playerId: string | null): void;
    setCameraViewMode(mode: ReplayCameraViewMode): void;
    setFreeCameraPreset(preset: ReplayFreeCameraPreset): void;
    setBallCamEnabled(enabled: boolean): void;
    setBoostMeterEnabled(enabled: boolean): void;
    setBoostPickupAnimationEnabled(enabled: boolean): void;
    setHitboxWireframesEnabled(enabled: boolean): void;
    setHitboxOnlyModeEnabled(enabled: boolean): void;
    setSkipPostGoalTransitionsEnabled(enabled: boolean): void;
    setSkipKickoffsEnabled(enabled: boolean): void;
    seek(time: number): void;
    setFrameIndex(frameIndex: number): void;
    stepFrames(delta: number): void;
    stepForwardFrame(): void;
    stepBackwardFrame(): void;
    setState(nextState: ReplayPlayerStatePatch): void;
    getState(): ReplayPlayerState;
    getSnapshot(): ReplayPlayerSnapshot;
    getTimelineDuration(): number;
    getTimelineCurrentTime(): number;
    getTimelineSegments(): ReplayPlayerTimelineSegment[];
    projectReplayTimeToTimeline(replayTime: number): ReplayPlayerTimelineProjection;
    projectTimelineTimeToReplay(timelineTime: number): number;
    private clampReplayTime;
    private getPlaybackEndTime;
    subscribe(listener: ReplayPlayerListener): () => void;
    onBeforeRender(callback: BeforeRenderCallback): () => void;
    addPlugin(definition: ReplayPlayerPluginDefinition): () => void;
    removePlugin(id: string): boolean;
    getPlugins(): ReplayPlayerPlugin[];
    destroy(): void;
    dispose(): void;
    private installResizeHandling;
    private scheduleAnimationFrame;
    private reanchorPlaybackClock;
    private setHitboxVisualizationVisibility;
    private syncPlaybackClock;
    private tick;
    private render;
    private skipPastKickoffIfNeeded;
    private skipPostGoalTransitionIfNeeded;
    private getActiveMetadata;
    private computeTimelineSegments;
    private installPlugin;
    private createPluginContext;
    private createPluginStateContext;
    private createRenderContext;
    private emitChange;
}
export {};
//# sourceMappingURL=player.d.ts.map